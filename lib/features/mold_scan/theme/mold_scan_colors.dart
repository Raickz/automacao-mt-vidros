import 'package:flutter/material.dart';

/// Dark palette for the mold-scanning feature, sampled directly from the
/// Marabá Temper logo (navy background #013D59, teal mark ~#1BBFBB) so this
/// screen reads as an official MT Vidros tool rather than a generic dark UI.
class MoldScanColors {
  static const background = Color(0xFF013D59);
  static const surface = Color(0xFF0A4A6B);
  static const surfaceLight = Color(0xFF11597D);
  static const border = Color(0xFF1D6C8F);
  static const accent = Color(0xFF1BBFBB);
  static const accentAlt = Color(0xFF29D6C7);
  static const textPrimary = Color(0xFFE8F4F8);
  static const textSecondary = Color(0xFFA9C4D1);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
}
