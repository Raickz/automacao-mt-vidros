import 'package:flutter/material.dart';

/// Dark, "industrial software" palette for the mold-scanning feature,
/// deliberately distinct from the rest of the app's light Material theme —
/// this screen is meant to read as a dedicated professional tool, matching
/// the visual register of the competitor product it was benchmarked against.
class MoldScanColors {
  static const background = Color(0xFF0B1220);
  static const surface = Color(0xFF131B2E);
  static const surfaceLight = Color(0xFF1B2740);
  static const border = Color(0xFF243149);
  static const accent = Color(0xFF2DD4BF);
  static const accentAlt = Color(0xFF22D3EE);
  static const textPrimary = Color(0xFFE5E7EB);
  static const textSecondary = Color(0xFF94A3B8);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
}
